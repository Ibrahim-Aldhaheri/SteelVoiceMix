[Unit]
Description=Nova Mixer GUI (ChatMix tray + overlay)
PartOf=graphical-session.target
After=graphical-session.target

[Service]
Type=simple
ExecStart=%h/.local/bin/nova-mixer-gui
Restart=on-failure
RestartSec=3

[Install]
WantedBy=graphical-session.target
